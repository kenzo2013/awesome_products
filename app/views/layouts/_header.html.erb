<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
	<div class="container-fluid">
		<a class="navbar-brand" href="/">Awesome Products</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav ms-auto">
				<li class="nav-item">
					<%= link_to 'Ajouter un produit', new_product_path, class: "nav-link active", "aria-current": "page" %>
				</li>
				<% if user_signed_in? %>
					<li class="nav-item">
						<%= link_to("Déconnexion", destroy_user_session_path, method: :delete, class: "nav-link active", "aria-current": "page" ) %>
					</li>
				<% else %>
					<li class="nav-item">
						<%= link_to('Connexion', new_user_session_path, class: "nav-link active", "aria-current": "page" ) %>
					</li>
				<% end %>
			</ul>
		</div>
	</div>
</nav>